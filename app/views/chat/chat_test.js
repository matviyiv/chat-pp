'use strict';

describe('chatApp.chat module', function() {

  beforeEach(module('chatApp.chat'));

  describe('chat controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ChatCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});